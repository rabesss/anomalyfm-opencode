.superpowers/
.codegraph/
ground-truth/
node_modules/
*.log
backends/
# No lockfile: this is an opencode plugin resolved through the host's own
# dependency tree, so a committed bun.lock would be noise rather than a
# reproducibility contract. CI/dev installs resolve fresh each run.
bun.lock

scripts/live-smoke.ts
results-verify-pause.csv

scripts/smoke-load.ts

docs/superpowers/
.kiro/
.claude/
.gemini/skills/
.github/skills/
.agents/state/
.desloppify/
.kilocode/
